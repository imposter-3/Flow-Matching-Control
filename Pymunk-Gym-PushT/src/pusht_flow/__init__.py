"""Flow matching action-chunk policies on state-based Push-T.

Four methods share a network, a training recipe and an evaluation protocol, and
differ only in how the flow's source distribution is built. The package
reproduces the main comparison at one network evaluation per replan. See
README.md for the commands.
"""

__version__ = "1.0.0"
